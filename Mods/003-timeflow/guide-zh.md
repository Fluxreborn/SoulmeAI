# 003 - Timeflow / 时相

**功能说明**：检测用户认知能量节律，提供时间天气预报与决策时机建议。

---

## Soul 字段

```
### 时相系统（Shixiang / Timeflow）
检测用户认知能量节律，提供时间天气预报与决策时机建议。

**核心公式**：
- λ = ρ / ρ̄（时间变换系数）
- τ = Σ(λ × Δt)（业务时间累积）
- 天气由 λ 判定，F（疲劳度）辅助预测恢复时间

**触发**："时相"、"timeflow"、"时间天气"、"我现在状态如何"等

**每次触发时执行**：
1. 运行 `~/.soulmeai/mods/timeflow/sync.py` 从 OpenClaw 会话同步最新交互数据
2. 读取 `storage/{user_id}/profile.json` 和 `history.jsonl`
3. 计算当前 λ 和 F 值
4. 判定天气状态并返回

**详见**：`METHODOLOGY.md`（算法细节、decay参数、Schema规范等）
```

---

## 快速开始

### 1. 首次使用（冷启动）

**用户说**："分析我的时相"

**Agent 执行**：
1. 检查 `storage/{user_id}/profile.json` 是否存在
2. 不存在 → 执行冷启动分析
3. 生成用户画像，返回模式类型（晨型/夜型/下午型）

### 2. 日常查询

**用户说**："我现在状态如何"

**Agent 执行**：
1. 检查当前小时交互数
2. **数据充足**（≥3条）：正常计算 λ → 判定天气
3. **数据稀疏**（<3条）：用 F 推断 λ → 判定天气（标注"预估中"）
   - *为什么是3条：统计意义上，3条是估算 ρ 的最小样本，低于此用 F 推断更准确*
4. 返回完整状态

**Agent 返回（数据充足）**：
```
🌧️ 小雨（λ=1.3，时间膨胀 1h→1.3h）
├─ 急性疲劳：F_acute=18（参考值）
├─ 慢性疲劳：F_chronic=24（参考值）
├─ 预计恢复：16小时后 λ 回升至 2+（阴天状态）
└─ 建议：轻量任务，避免重大决策，明日8点可恢复正常
```

**Agent 返回（数据稀疏）**：
```
☀️ 晴朗（基于恢复状态预估，F=8）
├─ 实时校准中（已收集 1/3 条交互）...
├─ 置信度：中等（基于疲劳度推断）
├─ 预计 2-3 条交互后确认实际状态
└─ 建议：可开始轻度工作，稍后确认深度工作时机
```

### 3. 今日预报

**用户说**："今日时相预报"

**Agent 返回全天天气流**：
```
📅 今日预报 | 周五

当前：🌧️ 小雨（λ=1.3）疲劳累积中

08:00-09:30 ☀️ 晴朗（预计 λ→12）深度工作
  └─ 基于：F_acute 夜间恢复至 <10

09:30-11:00 ⛅ 多云（预计 λ→6）创意产出
  └─ 基于：上午工作开始累积疲劳

11:00-12:30 ☁️ 阴天（预计 λ→3）事务处理
  └─ 基于：F_acute 持续增长
```

---

## 核心概念

### 时间膨胀（核心）

**λ = ρ / ρ̄**（时间变换系数）
- **ρ = 当前信息密度** = 有效交互次数 / 小时
  - **有效交互**：需认知资源的互动（提问、决策、深度回复、复杂任务执行）
  - **排除**：简单确认（"好的"/"明白"）、表情回复、系统消息、被动接收
- **ρ̄ = 个人历史平均密度**（7天滑动平均，冷启动默认值1.5）

**τ = Σ(λ × Δt)**（业务时间累积）
- 1物理小时 ≠ 1业务小时
- λ > 10：1小时深度工作 = 10+小时有效产出
- λ < 1：5小时发呆 = 1小时有效产出

### 双层疲劳模型（辅助预测）

用于预测未来的 λ 变化，解释为什么今天的状态会影响明天。

**F_acute**（急性疲劳）：短期累积，睡眠后快速恢复  
**F_chronic**（慢性疲劳）：长期累积，需要数天恢复  
**F_total = F_acute + F_chronic**

```
F_acute(t) = F_acute(t-1) × decay_acute + λ(t)
F_chronic(t) = F_chronic(t-1) × decay_chronic + transfer_rate × F_acute_peak
```

### 天气状态（基于 λ，核心）

| 状态 | 图标 | λ 值 | 时间膨胀 | 能量 | 建议任务 |
|:---|:---:|:---:|:---:|:---:|:---|
| 晴朗 | ☀️ | > 10 | 1h → 10h+ | 95% | 深度工作、复杂决策 |
| 多云 | ⛅ | 5-10 | 1h → 5-10h | 65% | 创意产出、写作 |
| 阴天 | ☁️ | 2-5 | 1h → 2-5h | 40% | 事务处理、邮件 |
| 小雨 | 🌧️ | 1-2 | 1h → 1-2h | 25% | 轻量任务、准备 |
| 静雾 | 🌫️ | < 1 | 1h → <1h | 15% | 休息、恢复 |

### 疲劳度参考（F_total = F_acute + F_chronic，辅助）

| F_total 范围 | 含义 | 对 λ 的影响 |
|:---:|:---|:---|
| < 10 | 轻度疲劳 | λ 可维持高位（>5）|
| 10-20 | 中度疲劳 | λ 开始下降（2-5）|
| 20-35 | 明显疲劳 | λ 显著下降（1-2）|
| 35-50 | 深度疲劳 | λ 很低（<1）|
| ≥ 50 | 极度疲劳 | λ 接近 0，需要长时间恢复 |

---

## 数据存储

```
storage/{user_id}/
├── history.jsonl       # 交互日志（append-only）
└── profile.json        # 用户画像
```

**隐私**：数据仅本地存储，不上传。

---

## 维护规则

- 交互自动记录到 `history.jsonl`
- 每7天或100条记录自动合并更新 `profile.json`
- **14天后自动提取个性化疲劳参数**（transfer_rate, decay_chronic）
- 冷启动时若数据不足50条，使用默认参数（基于实证研究）

---

## 详细文档

- **方法论**：`METHODOLOGY.md`（核心算法、执行流程、Schema 规范）
- **用户说明**：`README.md`
- **演示数据**：`data/profile_demo.json`、`data/history_demo.jsonl`

---

*知时相，善用光。*
